/target/
# Release builds are deliberately shipped with the plugin. Omarchy does not
# execute install hooks when it clones a plugin, so users must not need Cargo
# just to get the playback backend on first launch.
!/dist/
!/dist/*
!/dist/x86_64/
!/dist/x86_64/*
!/dist/x86_64/omarchy-spotify-backend
